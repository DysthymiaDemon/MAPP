<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/textColorWhite">

    <EditText
        android:id="@+id/reportTitleEditText"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="38dp"
        android:layout_marginTop="150dp"
        android:layout_marginRight="38dp"
        android:layout_marginBottom="92dp"
        android:hint="@string/title"
        app:layout_constraintBottom_toTopOf="@+id/reportDetailEditText"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/reportDetailEditText"
        android:layout_width="350dp"
        android:layout_height="145dp"
        android:layout_marginBottom="100dp"
        android:gravity="start|top"
        android:hint="@string/details"
        android:inputType="textMultiLine"
        app:layout_constraintBottom_toTopOf="@+id/submitRepButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.491"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/submitRepButton"
        android:layout_width="165dp"
        android:layout_height="45dp"
        android:layout_marginBottom="148dp"
        android:background="@color/colorPrimary"
        android:onClick="submitReport"
        android:text="@string/submit"
        android:textColor="@color/textColorWhite"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>